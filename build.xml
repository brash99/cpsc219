<project>

	<target name="clean">
		<delete dir="build"/>
	</target>

	<target name="compile">
		<mkdir dir="build/classes"/>
        	<javac srcdir="InitialSet" destdir="build/classes"/>
    	</target>

</project>
